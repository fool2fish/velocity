$user
$user.favorites[0]
$user.favorites[$favIdx]
$method.foo($arg).bar

$user.name.length()
$user.favorites.size()
$order.keySet()
$order.orderId

#set($temp = 1)
#set($addr = $order.addr)
$addr.zipCode
$addr.city
$addr.country

#foreach($employee in $employeeList)
$foreach.index: $employee.name - $employee.email
$employee.coding()
    #foreach($son in $employee.sonlist)
        $son.sonname
        $son.sonemail
        $son.coding()
        #foreach($grandson in $son.grandsonlist)
            $grandson.grandsonname
            $grandson.grandsonemail
            $grandson.coding()
        #end
    #end
#end

#parse('/child.vm')

#define($userInfo)
$user.name
$user.email
#end
$userInfo

#macro(hello $date)
$date.year $date.month $date.day
#end
#hello($today)
